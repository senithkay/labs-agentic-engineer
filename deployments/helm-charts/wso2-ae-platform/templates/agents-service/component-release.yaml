apiVersion: openchoreo.dev/v1alpha1
kind: ComponentRelease
metadata:
  name: agents-service-{{ include "ae-platform.tagSuffix" .Values.agentsService.image.tag }}
  namespace: {{ .Release.Namespace }}
spec:
  owner:
    projectName: asdlc-platform
    componentName: agents-service
  componentType:
    kind: ClusterComponentType
    name: platform-service
    spec:
      {{- include "ae-platform.platformServiceSpec" . | nindent 6 }}
  workload:
    container:
      image: "{{ .Values.agentsService.image.repository }}:{{ .Values.agentsService.image.tag }}"
    endpoints:
      http:
        type: HTTP
        port: {{ .Values.agentsService.port }}
        visibility:
          - project
