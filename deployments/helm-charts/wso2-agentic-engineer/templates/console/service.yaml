apiVersion: v1
kind: Service
metadata:
  name: asdlc-console
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: asdlc-console
  ports:
    - name: http
      port: {{ .Values.console.port }}
      targetPort: {{ .Values.console.port }}
